{
  "name": "calendar-project",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "concurrently \"npm start --prefix client\" \"npm run dev --prefix server\"",
    "install-all": "npm install && cd client && npm install && cd ../server && npm install"
  },
  "dependencies": {
    "calendar-project": "file:",
    "concurrently": "^8.2.2"
  }
}
